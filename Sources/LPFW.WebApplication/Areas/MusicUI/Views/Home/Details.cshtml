@model HomeDetailsViewModel
@{

    ViewBag.Title = "学生详情页";
    var photoPath = "~/images/" + (Model.Student.PhotoPath??"noimage.png");

}
    <div class="row justify-content-center m-3">
        <div class="col-sm-8">
            <div class="card">
                <div class="card-header">
                    <h1>@Model.Student.Name</h1>
                </div>

                <div class="card-body text-center">
                    <img class="card-img-top" src="@photoPath" asp-append-version="true" />

                    <h4>歌曲ID : @Model.Student.Id</h4>
                    <h4>歌曲名称 : @Model.Student.Name</h4>
                    <h4>歌手名称 : @Model.Student.SingerName</h4>
                    <h4>类型名称 : @Model.Student.TypeName</h4>
                </div>
                <div class="card-footer text-center">
                    <a  asp-action="Index" asp-controller="home" class="btn btn-primary">返回</a>
                    <a href="#" class="btn btn-primary">编辑</a>
                    <a href="#" class="btn btn-danger">删除</a>
                </div>
            </div>
        </div>
    </div>



@section Scripts{
    @*<script src="~/js/CustomScript.js"></script>*@
}

